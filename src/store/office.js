const officeSlice = (set, get) => ({
  emailHovered: false,
  instaHovered: false,
  linkedinHovered: false,
  githubHovered: false,
  socialsHovered: false,
});

const actions = (set, get) => ({});

export { actions, officeSlice };
